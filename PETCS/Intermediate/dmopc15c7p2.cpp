#include <bits/stdc++.h>
using namespace std;
int cnt = 0;
int main() {
    cin.sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    string s; 
    while(cin >> s) {
        cnt++;
    }
    cout << cnt << "\n";
    return 0;
}
